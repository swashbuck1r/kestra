apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: build
on:
  workflow_dispatch:
  push:
    branches:
      - develop
jobs:
  build:
    steps:
      - uses: cloudbees-io/checkout@v1
        name: checkout
      - uses: docker://gradle:jdk21
        name: gradle build
        kind: build
        shell: sh
        run: |
          echo "TODO: run a real build of this repo..."
          # ./gradlew build
          # find .
